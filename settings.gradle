dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'anki'

include(':shared')
include(':core-api')
include(':core')
include(':auth')

include(':api')
include(':cmd')
