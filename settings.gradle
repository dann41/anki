dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'anki'

include(':shared')
include(':core')
include(':core-api')
include(':auth')

include(':api')
include(':cmd')
